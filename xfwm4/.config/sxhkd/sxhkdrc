super + Return
    alacritty

super + p
    alacritty -e htop

super + r
    dmenu_run

super + e
    emacsclient -c

super + v
    clipcat-menu

Print
    screenie

XF86Audio{Lower,Raise}Volume
    volumectl {lower,raise}

super + bracket{left,right}
    volumectl {lower,raise}

ctrl + alt + q
    pkill xfwm4

ctrl + alt + r
    pkill -SIGUSR1 sxhkd
