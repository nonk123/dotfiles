super + Return
    alacritty

super + p
    alacritty -e htop

super + r
    dmenu_run

super + v
    clipcat-menu

Print
    flameshot gui -c

shift + Print
    flameshot screen -c

XF86Audio{Lower,Raise}Volume
    volumectl {lower,raise}

super + bracket{left,right}
    volumectl {lower,raise}

ctrl + alt + q
    pkill xfwm4

ctrl + alt + r
    pkill -SIGUSR1 sxhkd
