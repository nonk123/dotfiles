#!/bin/sh

# Download the git-latest version of `winetricks`, and install it in PATH.

# No args.

INSTALL_PATH=~/.local/bin/winetricks
DOWNLOAD_URL=https://raw.githubusercontent.com/Winetricks/winetricks/master/src/winetricks

wget -O $INSTALL_PATH $DOWNLOAD_URL
chmod +x $INSTALL_PATH
