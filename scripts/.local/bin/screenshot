#!/usr/bin/env bash

case $1 in
    region)
        maim -s
        ;;
    display)
        maim
        ;;
    *)
        echo 'Pass "region" or "display"' > /dev/stderr
        exit 1
esac | xclip -quiet -sel clip -t image/png
