# key: c
# name: class
# --

package `(replace-regexp-in-string "/" "." (file-relative-name (file-name-directory buffer-file-name)) (or (projectile-project-root) ".")))`;

public class `(file-name-base (buffer-file-name))` {

    $0
}
