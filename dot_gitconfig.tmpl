[user]
	name = nonk123
	email = me@nonk.dev
	signingkey = me@nonk.dev
[github]
	user = nonk123
[commit]
	gpgsign = true
[pull]
	rebase = true
{{ if eq .chezmoi.os "windows" -}}
[gpg]
	program = {{ joinPath .chezmoi.homeDir "AppData\\\\Local\\\\Programs\\\\GnuPG\\\\bin\\\\gpg.exe" | quote }}
[http]
	sslBackend = openssl
{{- end }}
